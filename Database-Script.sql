-- ============================================================
-- RaceDay - SQL Database Script
-- STATUS: PLACEHOLDER - NOT YET COMPLETE
--
-- This file was auto-generated only so that the Part 1
-- repository-structure CI check has a real file to validate
-- against. It must be replaced with the actual RaceDay
-- database creation script (tables, keys, constraints) before
-- this counts as a completed Part 1 deliverable.
--
-- This script is NOT executed by CI. It is a planning
-- artefact only, matching the ERD in docs/RaceDay-ERD.md.
-- ============================================================

-- Example structure only - replace with your real schema:

-- CREATE TABLE Users (
--     UserId INT PRIMARY KEY IDENTITY(1,1),
--     Name NVARCHAR(100) NOT NULL,
--     Email NVARCHAR(100) NOT NULL UNIQUE
-- );

-- CREATE TABLE Races (
--     RaceId INT PRIMARY KEY IDENTITY(1,1),
--     Name NVARCHAR(100) NOT NULL,
--     RaceDate DATE NOT NULL
-- );

-- CREATE TABLE Entries (
--     EntryId INT PRIMARY KEY IDENTITY(1,1),
--     RaceId INT NOT NULL FOREIGN KEY REFERENCES Races(RaceId),
--     UserId INT NOT NULL FOREIGN KEY REFERENCES Users(UserId)
-- );
