# RaceDay – API Endpoint Plan

> **STATUS: PLACEHOLDER — NOT YET COMPLETE**
> This file was auto-generated only so that the Part 1 repository-structure CI check has a real file to validate against. It must be replaced with the actual planned API endpoints before this counts as a completed Part 1 deliverable.

## What needs to go here

A table or list of the planned API endpoints for the RaceDay application, for example:

| Method | Endpoint | Description | Auth Required |
|--------|----------|-------------|----------------|
| GET    | /api/races | List all races | No |
| POST   | /api/races | Create a new race | Yes |
| GET    | /api/races/{id} | Get a single race by ID | No |
| PUT    | /api/races/{id} | Update a race | Yes |
| DELETE | /api/races/{id} | Delete a race | Yes |
| GET    | /api/entries | List race entries | Yes |
| POST   | /api/entries | Create a race entry | Yes |

Replace the example rows above with your actual planned resources (races, entries, results, users, venues, etc.) and endpoints, matching your ERD.
