# RaceDay – Entity Relationship Diagram (ERD)

> **STATUS: PLACEHOLDER — NOT YET COMPLETE**
> This file was auto-generated only so that the Part 1 repository-structure CI check has a real file to validate against. It must be replaced with the actual RaceDay ERD before this counts as a completed Part 1 deliverable.

## What needs to go here

Replace this placeholder with the real Entity Relationship Diagram for the RaceDay database, showing:

- All core entities (e.g. Users, Races, Entries, Results, Venues — adjust to your actual design)
- Primary keys and foreign keys for each entity
- Relationships and cardinality between entities (1:1, 1:many, many:many)

## Suggested formats

- An exported diagram image (`.png` / `.jpg`) embedded below, or
- A PDF export of the diagram, or
- A diagram-as-code source file (e.g. `.drawio`, `.dbml`, Mermaid `erDiagram` block) rendered inline

If you change the file name or format, update the corresponding check in `.github/workflows/ci.yml` to match.
